describe : it is also called suite : consists of group of related tests/specs

        describe("description", () => {
          // specs
          it("spec1", () => {
              expect(true).toBe(true)
          });

          it(...);
          ...

        })
